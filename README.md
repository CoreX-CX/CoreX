Updated README with admin setup details.

- Added instructions to create an admin user in Supabase and set ADMIN_EMAILS env var.
- Ensure `requests` table includes a `completed` boolean column (default false).
